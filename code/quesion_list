1.  两个变量a和b，不使用任何条件判断语句，找出两个数变量中最大的一个？
2.  对数组[45, 38, 65, 97, 76, 13, 27, 49]进行快速排序，第二次划分完成后的数组是？
3.  对数组a[0, m]和a[m+1, n+n]有分别有序，将其进行merge操作成有序数组a[0, m+n]？
4.  定义栈的数据结构，要求添加一个min函数，能够得到栈的最小元素。要求min以及Push、Pop的时间复杂度都是O(1).
5.  
